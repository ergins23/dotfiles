#!/bin/bash

rm -f ~/.bash_profile
ln -s ./bash_profile ~/.bash_profile

rm -f ~/.bashrc
ln -s ./bashrc ~/.bashrc

rm -f ~/.emacs
ln -s ./emacs ~/.emacs

rm -f ~/.gitconfig
ln -s ./gitconfig ~/.gitconfig

rm -f ~/.gitignore
ln -s ./gitignore ~/.gitignore
