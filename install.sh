#!/bin/bash

rm -f ~/.bash_profile
ln ./bash_profile ~/.bash_profile

rm -f ~/.bashrc
ln ./bashrc ~/.bashrc

rm -f ~/.emacs
ln ./emacs ~/.emacs

rm -f ~/.gitconfig
ln ./gitconfig ~/.gitconfig

rm -f ~/.gitignore
ln ./gitignore ~/.gitignore
